# Placeholder requirements list for tooling that expects a pip-compatible file.
# This repository stores its textual requirements catalog in requirements-catalog.txt instead.
# No packages are specified here intentionally.
